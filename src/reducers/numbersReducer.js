//import {} from "../actions";

const initialState = {
  numbers: null,
  isFetching: false,
  error: "";
  
}

const reducer = (state, initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;